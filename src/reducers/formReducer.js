/**
 * Redux form reducer
 * @module
 */

const formConstant = {}

const formReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default formReducer
